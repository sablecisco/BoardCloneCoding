spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/boardSample
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: "!@aufrhd12"
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace